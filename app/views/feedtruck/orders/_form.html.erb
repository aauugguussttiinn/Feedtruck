<%= form_with(model: foodtruck_order, local: true) do |form| %>
  <% if foodtruck_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(foodtruck_order.errors.count, "error") %> prohibited this foodtruck_order from being saved:</h2>

      <ul>
      <% foodtruck_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
