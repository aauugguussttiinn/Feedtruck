<%= form_with(model: foodtruck_foodtruck, local: true) do |form| %>
  <% if foodtruck_foodtruck.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(foodtruck_foodtruck.errors.count, "error") %> prohibited this foodtruck_foodtruck from being saved:</h2>

      <ul>
      <% foodtruck_foodtruck.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
